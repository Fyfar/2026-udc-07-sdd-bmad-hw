---
topic: Checkout Discounts (Loyalty Tiers + Promo Codes) PRD
updated: 2026-08-30T18:42
---

- (event) Headless/autonomous PRD run: no stakeholder available, PM (John) makes and records all decisions per brief-2026-udc-07-sdd-bmad-hw-2026-08-30.
- (decision) Adopted brief decisions 1-9 verbatim (sequential stacking, items-only base, category-subtotal coupon base, gross-subtotal min gate, apply-all codes, first-wins category conflict, explicit rejection reasons, per-line round-half-up, floor at 0).
- (assumption) GAP (brief silent): decision #1 (sequential compounding on 'the remaining amount') and #3 (category coupon computed off category subtotal) do not say how a whole-order-level discount (tier, or a non-category coupon) affects a LATER category-scoped coupon's base. Resolved: engine tracks a running remaining-subtotal PER CATEGORY BUCKET. Tier % and any non-category coupon reduce every bucket by the same percent; for a non-category FIXED coupon, split proportionally by each bucket's current share, round half-up per bucket, leftover kopeck to the bucket with the largest remaining value (ties by category name ascending). A category-scoped coupon reduces only its own bucket. 'Per line' in decision #8 means 'per category bucket, per discount-application step', not per physical LineItem row.
- (assumption) GAP (brief/types silent): pricing.ts has no 'current time' concept and no coupon catalog/store. Resolved: the discount engine's public function takes explicit params (order, coupons: Coupon[], asOf: Date) rather than reading Date.now() or an implicit store; asOf defaults to new Date() only at the call site, never inside the pure pricing function.
- (assumption) GAP (brief listed only 4 rejection reasons: expired, category_mismatch, min_not_met, category_claimed): a typed code with no matching Coupon record is a 5th case. Resolved: reason not_found.
- (assumption) GAP (brief says dedupe 'applies it once' but decision #7 demands explicit per-code outcome): resolved -- every entered occurrence gets an outcome; first occurrence validated normally, every later repeat (case-insensitive, trimmed) rejected with reason duplicate, never silently merged.
- (decision) Per-code validation order fixed: (1) exists in catalog -> not_found, (2) not expired as of asOf -> expired, (3) order gross item subtotal >= minSubtotalKopecks -> min_not_met, (4) category not already claimed by an earlier-applied code -> category_claimed, (5) not a repeat of an already-processed code -> duplicate. First failing check wins; codes processed in customer entry order.
- (decision) Open Question 'max codes per order' (brief left this to PM/architecture): resolved for v1 -- cap at 10 distinct codes per order (abuse/perf guard); excess beyond the 10th rejected with reason too_many_codes. [ASSUMPTION -- no data on real campaign code volume].
- (decision) Coupon record data integrity (percent 0-100, non-negative fixed value) assumed pre-validated at data-entry time per brief's out-of-scope 'coupon issuance/management tooling'; engine validates order-time applicability only, not record well-formedness.
- (event) PRD drafted directly (single-pass, autonomous) -- no web research spawned (internal engineering change, brief already skipped discovery research).
- (event) Self-review pass complete: fixed two garbled sentences in FR-2/FR-4 consequences, added no_matching_items reason to the fixed validation-order chain for consistency. Addendum written covering rejected alternatives for the 4 new PRD-level decisions (category buckets, proportional fixed-coupon split, explicit asOf/catalog params, extra rejection reasons).
- (event) PRD finalized. status: final, updated: 2026-08-30. No interactive reviewer subagents spawned (autonomous single-pass run per parent orchestrator instruction); self-reviewed for internal consistency instead.
